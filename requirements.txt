# WhatsApp -> OpenCode -> OpenRouter PoC backend
# Pinned to known-good releases for reproducible Railway builds.

fastapi==0.115.6        # ASGI web framework hosting the webhook
uvicorn[standard]==0.34.0  # ASGI server (used in the Dockerfile CMD)
pywa==2.8.0             # Official-style WhatsApp Cloud API wrapper
pymongo[srv]==4.10.1    # MongoDB driver (+srv pulls in dnspython for Atlas URIs)
discord.py==2.4.0       # Discord bot adapter (group @mentions + DMs)
